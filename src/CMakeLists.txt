# Edge Sensor
SET(EDGE_MQTT_SRCS ${EDGE_MQTT_SRC_DIR}/ml-edge-mqtt.c)
ADD_LIBRARY(capi-edge-mqtt SHARED ${EDGE_MQTT_SRCS})
SET_TARGET_PROPERTIES(capi-edge-mqtt PROPERTIES VERSION ${SO_VERSION})
TARGET_INCLUDE_DIRECTORIES(capi-edge-mqtt PRIVATE ${INCLUDE_DIR})
TARGET_LINK_LIBRARIES(capi-edge-mqtt ${EDGE_REQUIRE_PKGS_LDFLAGS})

INSTALL (TARGETS capi-edge-mqtt DESTINATION ${CMAKE_INSTALL_LIBDIR})
INSTALL (FILES ${INCLUDE_DIR}/ml-edge-mqtt.h DESTINATION ${INCLUDE_INSTALL_DIR})
